# WorkVouch Environment Variables
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to git - it contains sensitive keys!

# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# In Supabase Dashboard → Auth → URL Configuration, set Redirect URLs to include:
#   https://tryworkvouch.com, https://*.vercel.app

# ============================================================================
# NEXTAUTH CONFIGURATION (required for production)
# ============================================================================
# Production: NEXTAUTH_URL=https://tryworkvouch.com (set in Vercel Environment Variables)
# Local: NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_URL=https://tryworkvouch.com

# Required. Generate once with: openssl rand -base64 32 — keep stable (do not regenerate).
NEXTAUTH_SECRET=your-generated-secret-here-minimum-32-bytes

# ============================================================================
# STRIPE CONFIGURATION
# ============================================================================
# Stripe Keys (get from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_...  # Use sk_live_... for production
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...  # Use pk_live_... for production
STRIPE_WEBHOOK_SECRET=whsec_...  # Get from Stripe Dashboard → Webhooks

# ============================================================================
# STRIPE PRICE IDs (Canonical Employer Plans: Lite, Pro, Enterprise)
# ============================================================================
# Get these from Stripe Dashboard → Products → [Product] → Pricing
# Each Price ID starts with "price_"

# Employer Plans (single source: lib/pricing/employer-plans.ts)
NEXT_PUBLIC_STRIPE_LITE_MONTHLY=price_...      # Lite $29/mo
NEXT_PUBLIC_STRIPE_PRO_MONTHLY=price_...       # Pro $79/mo
NEXT_PUBLIC_STRIPE_ENTERPRISE_MONTHLY=price_... # Enterprise $199/mo
# Server-side (optional; falls back to NEXT_PUBLIC_*)
# STRIPE_PRICE_LITE_MONTHLY=price_...
# STRIPE_PRICE_PRO_MONTHLY=price_...
# STRIPE_PRICE_ENTERPRISE_MONTHLY=price_...

# One-Time Payment
STRIPE_PRICE_ONE_TIME=price_... # One-time report (optional)

# Metered overage (add these prices to subscription for overage billing)
# STRIPE_PRICE_REPORT_OVERAGE=price_...
# STRIPE_PRICE_SEARCH_OVERAGE=price_...
# STRIPE_PRICE_SEAT_OVERAGE=price_...

# ============================================================================
# OPTIONAL SERVICES
# ============================================================================

# Google OAuth (only if using Google sign-in)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# SendGrid (only if sending emails)
# SENDGRID_API_KEY=SG...
# SENDGRID_SENDER_EMAIL=your-verified-email@example.com

# OpenAI (only if using AI features)
# OPENAI_API_KEY=sk-...

# ============================================================================
# APPLICATION URL (use ONE canonical domain for cookie/session stability)
# ============================================================================
NEXT_PUBLIC_URL=http://localhost:3000  # For production: https://tryworkvouch.com
# Canonical site URL — do not mix www/non-www or vercel.app with custom domain
NEXT_PUBLIC_SITE_URL=http://localhost:3000  # Production: https://yourdomain.com

# ============================================================================
# SIMULATION MODE (Admin / Investor Demo only)
# ============================================================================
# Set to "true" to enable simulation content at /admin/investor-demo.
# Public routes never show simulation. Leave unset or "false" in production.
# NEXT_PUBLIC_SIMULATION_MODE=true
