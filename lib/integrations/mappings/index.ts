/** Canonical mapping utilities — field mapping contracts live in docs/integration-contract/. */
export type {
  CanonicalCandidate,
  CanonicalJob,
  CanonicalApplication,
  ApplicationStatus,
} from "../types/sync";
