/**
 * Centralized Supabase mutations (insert, update, delete).
 * Add modules here as you migrate from inline Supabase calls.
 *
 * Example:
 * export { insertJob } from "./insertJob";
 * export { updateProfile } from "./updateProfile";
 */
